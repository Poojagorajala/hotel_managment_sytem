<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rooms by Hotel</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <header>
        <nav>
            <ul>
                <li><a href="/">Home</a></li>
                <li><a href="/rooms">Rooms</a></li>
            </ul>
        </nav>
        <h1>Rooms for Hotel <%= roomsbyhotel && roomsbyhotel.length > 0 ? roomsbyhotel[0].hotel_id : 'N/A' %></h1>
    </header>

    <main class="container">
        <h2>Rooms in this Hotel</h2>
        <% if (roomsbyhotel && roomsbyhotel.length > 0) { %>
            <table>
                <thead>
                    <tr>
                        <th>Room ID</th>
                        <th>Room Type</th>
                        <th>Price</th>
                        <th>Availability</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% roomsbyhotel.forEach(room => { %>
                        <tr>
                            <td><%= room.room_id %></td>
                            <td><%= room.room_type %></td>
                            <td><%= room.price %></td>
                            <td><%= room.availability %></td>
                            <td class="actions">
                                <a href="/rooms/<%= room.room_id %>/details">Details</a>
                                <a href="/rooms/<%= room.room_id %>/edit">Edit</a>
                                <form action="/rooms/<%= room.room_id %>/delete" method="POST" style="display: inline;">
                                    <button type="submit" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this room?')">Delete</button>
                                </form>
                            </td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        <% } else { %>
            <p>No rooms found for this hotel.</p>
        <% } %>
    </main>

    <footer>
        <p>&copy; <%= new Date().getFullYear() %> My Awesome Hotel App</p>
    </footer>
</body>
</html>