<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <title>Edit Hotel</title>
    <link rel="stylesheet" href="/style.css" />
</head>
<body>
    <header>
        <nav>
            <ul>
                <li><a href="/">Home</a></li>
                <li><a href="/hotels">Hotels</a></li>
            </ul>
        </nav>
        <h1>Edit Hotel</h1>
    </header>

    <main class="container">
        <form action="/hotels/<%= hotel.hotel_id %>/update" method="POST">
            <label for="name">Hotel Name:</label>
            <input
                type="text"
                id="name"
                name="name"
                value="<%= hotel.name %>"
                required
                maxlength="30"
            />

            <label for="address">Address:</label>
            <input
                type="text"
                id="address"
                name="address"
                value="<%= hotel.address %>"
                required
                maxlength="30"
            />

            <label for="city">City:</label>
            <input
                type="text"
                id="city"
                name="city"
                value="<%= hotel.city %>"
                required
                maxlength="30"
            />

            <button type="submit" class="btn btn-primary">Update Hotel</button>
            <a href="/hotels" class="btn btn-secondary">Cancel</a>
        </form>
    </main>

    <footer>
        <p>&copy; <%= new Date().getFullYear() %> Hotel Management System</p>
    </footer>
</body>
</html>
