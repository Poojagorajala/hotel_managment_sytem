<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Details</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <header>
        <nav>
            <ul>
                <li><a href="/">Home</a></li>
                <li><a href="/payments">Payments</a></li>
            </ul>
        </nav>
        <h1>Payment Details</h1>
    </header>

    <main class="container">
        <h2>Payment Details</h2>
        <% if (paymentid) { %>
            <p><strong>Payment ID:</strong> <%= paymentid.payment_id %></p>
            <p><strong>Booking ID:</strong> <%= paymentid.booking_id %></p>
            <p><strong>Amount:</strong> <%= paymentid.amount %></p>
            <p><strong>Payment Status:</strong> <%= paymentid.payment_status %></p>
            <a href="/payments/<%= paymentid.payment_id %>/edit" class="btn btn-primary">Edit Payment</a>
            <form action="/payments/<%= paymentid.payment_id %>/delete" method="POST" style="display: inline;">
                <button type="submit" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this payment?')">Delete</button>
            </form>
        <% } else { %>
            <p>Payment not found.</p>
        <% } %>
    </main>

    <footer>
        <p>&copy; <%= new Date().getFullYear() %> My Awesome App</p>
    </footer>
</body>
</html>